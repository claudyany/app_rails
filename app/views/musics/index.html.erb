<p id="notice"><%= notice %></p>

<h1>Lista de Musicas</h1>

<table>
  <thead>
    <tr>
      <th>Artista</th>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @musics.each do |music| %>
      <tr>
        <td><%= music.artist %></td>
        <td><%= music.name %></td>
        <td><%= link_to 'Mostrar', music %></td>
        <td><%= link_to 'Editar', edit_music_path(music) %></td>
        <td><%= link_to 'Deletar', music, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova musica', new_music_path %>
